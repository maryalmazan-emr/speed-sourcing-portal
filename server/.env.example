# Server
PORT=3001
NODE_ENV=development

# CORS (comma-separated)
CORS_ORIGINS=http://localhost:5173

# Optional Azure SQL connection string (leave empty for in-memory mode)
# Example:
# AZURE_SQL_CONNECTION_STRING=Server=tcp:<server>.database.windows.net,1433;Initial Catalog=<db>;Persist Security Info=False;User ID=<user>;Password=<pass>;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;
AZURE_SQL_CONNECTION_STRING=